# Quantum Annealing Simulation with D-Wave Ocean

Simulates quantum annealing using **classical simulated annealing** via D-Wave's Ocean SDK.

## Problem
Quadratic Assignment Problem (QAP): Assign facilities to locations to minimize total cost.

## Run

```bash
pip install -r requirements.txt
python quantum_annealing_sim.py
